-- Expand-only: ชื่อผู้ปฏิบัติงานจริง — คนที่ลงมือทำกิจกรรมนั้นด้วยตัวเอง ซึ่งไม่ใช่
-- `recordedBy` (บัญชีที่กดบันทึก) เสมอไป · ฟาร์มที่จ้างแรงงาน สองคนนี้คนละคน และ
-- GACP ถามถึงคนที่สัมผัสผลผลิตจริง (สุขอนามัย/การอบรม) ไม่ใช่คนที่นั่งกรอก
--
-- Nullable, ไม่มี backfill: บันทึกที่มีอยู่แล้วไม่เคยถูกถามคำถามนี้ ⇒ ต้องอ่านว่า
-- "ไม่ทราบ" ไม่ใช่เดาว่าเป็นคนเดียวกับผู้บันทึก · ไม่บังคับกรอก (มติ operator แบบ ก.)
ALTER TABLE "cultivation_logs" ADD COLUMN IF NOT EXISTS "performedBy" TEXT;
