- 2026-08-03 | smoke test บน prod container: `payment-reminder-job.test.js:363` fail (expect 11 ได้ 22) เฉพาะเมื่อ `NODE_ENV=production` — root cause พิสูจน์แล้ว: `apps/backend/jobs/scheduler.js:289-291` auto-start ตอน require เมื่อ NODE_ENV≠test → ใน test โมดูลถูก require (start รอบ 1) + test เรียก `scheduler.start()` เอง (รอบ 2) = 22 = 11×2; ใน process production จริงลงทะเบียนรอบเดียว — **ไม่ใช่ bug production เป็น test-env artifact** | งานเสนอ: ทำ test ทนต่อ NODE_ENV ที่ถูก set มาก่อน (force NODE_ENV=test ใน setup หรือ reset registration ก่อนนับ) | พบระหว่าง MIGRATION CHECK ขั้น 4 (orchestrator reproduce เอง)
