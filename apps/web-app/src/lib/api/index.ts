// Export all API utilities from a single entry point
export { apiClient, ApiClient, type ApiResponse } from './api-client';

// Alias for convenience
export { apiClient as api } from './api-client';
